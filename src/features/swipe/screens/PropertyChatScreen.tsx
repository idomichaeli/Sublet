import React from "react";
import ChatListScreen from "../../favorites/screens/PropertyChatListScreen";

export default function ChatScreen({ navigation }: any) {
  return <ChatListScreen navigation={navigation} />;
}
